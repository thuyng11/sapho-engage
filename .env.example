# Optional. Relative paths resolve from the sapho-engage directory.
# The parent directory is created when needed. Defaults to sapho.db.
DATABASE_PATH=sapho.db

GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.7-flash
